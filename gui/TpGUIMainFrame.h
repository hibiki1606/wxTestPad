#pragma once

// Header of the GUI class which is generated by builder
#include "buildergen/TpGUI.h"

#include "notepad/Notepad.h"

class TpGUIMainFrame : public MainFrame {
   protected:
    Notepad m_notepad;
    
    void MainFrameOnClose(wxCloseEvent& event);

    bool OpenFileDialog();
    bool SaveFileDialog();
    bool FontDialog();

    void m_ribbonOnClick(wxRibbonButtonBarEvent& event);
    void m_ribbonToggleTheme(wxCommandEvent& event);

   public:
    TpGUIMainFrame(wxWindow* parent);
};